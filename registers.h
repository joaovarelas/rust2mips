#ifndef __registers_h__
#define __registers_h__

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int s_count,
    t_count,
    l_count;

int ndigits(int x);
char* sx();
char* tx();
char* lx();

#endif
