#ifndef __registers_h__
#define __registers_h__

#include <stdlib.h>
#include <stdio.h>
#include <math.h>
 
int t_count,
    s_count,
    l_count;

int ndigits(int x);
char* tx();
char* sx();
char* lx();

#endif
